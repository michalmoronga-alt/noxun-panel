# KOV-B — Codex audit návrhu pred implementáciou (3.9.2026, checkpoint #17)

> Stav: KONCEPT / audit checkpoint — nie implementačný spec. Codex CLI task `task-mtk9x1k8-cu4tnl` (gpt-5.6-sol, 24 min; prvý pokus `task-mtk2rznq-bu8hzt` padol na kvóte) nad package KOV-B + FINAL §7/§11 + mockup scéna 3, dôkazy pripnuté na main `a29bfa2` (pred KOV-H1). Verdikt: **4 BLOCKER + 10 FIX + 2 NOTE; rez B1/B2/B3 s exit kritériami.** Všetky blockery rozhodnuté orchestrátorom technicky; autorita dávok = packages KOV-B1/B2/B3 v PLAN.md.

## BLOCKERY + rozhodnutie orchestrátora

1. **Šablóny obchádzajú downgrade bránu setov** (`hardware_set_defs` idú cez tolerantný `normalize_sets`; starý plugin by zmrazil orezaný set). → **ROZHODNUTÉ (B1):** `CONFIG_SCHEMA` 3 → **4** (KOV-H1 vzala 3 — schémy sa prideľujú sekvenčne podľa poradia mergov, FIX 5); pri POUŽITÍ/VKLADE šablóny beží bezstratová kontrola definícií (`assess_set_defs` = ten istý whitelist/typový detektor ako `assess_library_doc`) — novší tvar = odmietnutie BEZ zápisu do modelu; test „šablóna z KOV-B je vo v0.9.18 odmietnutá a model sa nezmení".
2. **`use_type` nemá vzťah k povinnému `generic_type`; `lift/fall` sa nedajú uložiť.** → **ROZHODNUTÉ (B1):** jediná kanonická mapa `use_type → generic_type`: `door→hinge`, `drawer→slide`, `lift→lift`, `fall→lift`, `other→` explicitný `generic_type` (ľubovoľný zo slovníka). `generic_type` ostáva v dátach (odvodený server), `validate_set` (zápis) odmietne nekonzistentnú kombináciu, čítanie je tolerantné. **`GENERIC_TYPES + lift` sa presúva z KOV-E do B1** vrátane `plan_schema` bumpu (`guard_unknown_hardware!` chráni starší plugin) — KOV-E potom pridáva len pravidlá a dáta.
3. **Modal setu zdedí tichý prepis medzi dvoma oknami** (`HWS_EDIT` posiela čerstvú revíziu, nie pripnutú). → **ROZHODNUTÉ (B3):** pripnúť revíziu aj základnú definíciu setu pri otvorení modalu (vzor `hwsPinRev` editora mapovania); konflikt = vyžiadať obnovu/riešenie. Existujúci latentný bug zapísaný ako **R-41** v AUDIT_REGISTER (opraví ho B3).
4. **`JsonFileStore + .bak` nie je kompletný kontrakt taxonómie.** → **ROZHODNUTÉ (B1):** `hardware_taxonomy.json` = verzovaný dokument `{std, schema, manufacturers[], series[]}` s prísnym posúdením tvaru (`assess!` vzor `HardwareCatalog`), stavy `:ok/:degraded/:read_only` viditeľné v UI, fresh-read + revízia pod `Materials.with_catalog_lock` pred každým zápisom, `.bak`; API len **`create_manufacturer!` / `create_series!`** (patch, case-insensitive dedup, rada patrí presne jednému výrobcovi) — rename/delete mimo V1 (FIX 10).

## FIX-IN-KOVB (prijaté; zaradenie do rezu)

5. Unikátne čísla `CONFIG_SCHEMA` (H1 = 3 → B1 = 4) — B1. · 6. Klasifikácia = buď úplne chýba (legacy), alebo úplná a kontextovo platná (`drawer_construction` len pri `drawer`, `series` patrí `manufacturer`); čiastočné tvary server pri zápise odmietne — B1. · 7. `active` sparse (ukladá sa len `false`), legacy setom sa klasifikácia nikdy nedopĺňa, `snapshot_std` bumpne LEN pri prítomnom klasifikačnom poli (test: každé pole samostatne bumpne, čistý legacy ostáva na pôvodnom std) — B1. · 8. **Kanonický tvar mapovacieho kľúča pre KOV-D už v B1:** `class:<generic_type>|<opening_mode>[|<drawer_construction>]` (napr. `class:slide|tipon|metal`, `class:hinge|classic`), hodnota ako dnes (set_id/selector), `parse_mapping` ho pozná, round-trip + std detekcia (nová forma) — KOV-D už nebude potrebovať ďalší bump — B1. · 9. `active` ovplyvňuje len ponuku nového defaultu/výberu; existujúce mapovanie, snapshot a šablóna expandujú identicky (`expand`/`explain` `active` nečítajú) — B1 (data) + B3 (UI). · 10. Taxonómia bez „úplnej náhrady" (R-35): len create patch pod spoločným zámkom — B1/B2. · 11. Démos: `pid` proposal flow ostáva server-owned (kód/názov/cena/MJ), klient smie kategóriu/poznámku/výrobcu/radu; parser `brand` → kanonický výrobca cez taxonómiu na serveri; Tip-On len schváleným pravidlom; ručne zmenený Démos údaj nie je „overený" — B2. · 12. Serverový strom Kategória→Výrobca→Rada s `shown/total` na každej úrovni + „načítať ďalšie"; hľadanie indexuje výrobcu a radu; test s 500+ položkami nájde položku za poradím 200 a zachová `pin` — B2. · 13. D-15 kontrakt naplno: štruktúrované serverové chyby `{row, field, msg}` (aj zo `save_set!`), busy lock, modal otvorený pri odmietnutí, drafty bez server-owned `row_rev` — B3 (+ tvar chýb servera v B1). · 14. Živý náhľad expanzie: endpoint validuje odoslaný DRAFT, zostaví syntetického ownera a dočasné mapovanie bez IO, vracia request generation (staršia odpoveď neprepíše novšiu), test = zhoda s `expand` nad rovnakým vstupom — B3.

## NOTE

15. Rez a exit kritériá prijaté (viď packages B1/B2/B3 v PLAN.md); PR sekvenčne, ďalší rez až po splnení exit kritérií predchodcu. · 16. Dôkazy pripnuté na `a29bfa2`; medzitým pristála KOV-H1 (CONFIG_SCHEMA 3, `hardware_manual`) — subagent B1 začína krátkym read-only auditom proti aktuálnemu mainu.
