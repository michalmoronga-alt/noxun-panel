# KOV-B — prompt pre Codex audit návrhu (na zopakovanie po resete limitu 3.9.2026 17:32)

> Stav: KONCEPT / pomocný prompt — beh `task-mtk2rznq-bu8hzt` 3.9. ~14:35 padol na Codex usage limite uprostred (jediný zachytený nález nižšie). Spustiť znova cez skill `codex-audit` (PowerShell: `$prompt = Get-Content -Raw '<tento súbor bez tejto hlavičky>'; node <codex-companion.mjs> task --background $prompt`), po dobehnutí zapísať checkpoint `KOVANIE_KOVB_AUDIT_<dátum>_17.md` a prepísať package KOV-B v PLAN.md (rez B1/B2/B3 podľa nálezov).

## Zachytený nález z prerušeného behu

- **`use_type` vs. povinný `generic_type`:** editor podľa mockupu `generic_type` vôbec neukazuje, `lift` ešte nie je povolený `GENERIC_TYPE`
  (pribudne až KOV-E) a KOV-D chce mapovací kľúč `slide|hinge|lift × opening_mode [× drawer_construction]`. Bez **kanonickej mapy
  `use_type → generic_type`** (door→hinge, drawer→slide, lift/fall→lift, other→?) a **validačnej matice** by B zapisovalo sety, ktoré D nevie
  jednoznačne adresovať. → do package B1: `use_type` odvodzuje `generic_type` (jedno miesto), `lift`/`fall` sety sa v B nedajú založiť, kým
  `GENERIC_TYPES` nemá `lift` (alebo B pridá `lift` do `GENERIC_TYPES` s `plan_schema` bumpom — rozhodnúť v audite).

## Prompt (odovzdať Codexu bez zmien)

You are an adversarial design reviewer (devil's advocate) for the SketchUp Ruby plugin Noxun Engine (repo root: C:\APP DEV\RUBY\ENGINE, branch main, plugin ≥ v0.9.17). READ the actual files before judging — do not trust the summary.

DESIGN UNDER REVIEW — task package "KOV-B · KATALÓG A SETY — KLASIFIKÁCIA A EDITORY" (slice B of block KOVANIE, dogfooding item D-110). Read its full authoritative text in SYSTEM/PLAN.md (block "### KOVANIE", bullet "- **KOV-B · TASK PACKAGE"), plus the architecture source SYSTEM/zdroje/next_sessions/KOVANIE_V1_ARCHITEKTURA_2026-09-02_FINAL.md §7 (sety a procurement) and §11 (migrácia), and the approved UX mockup SYSTEM/zdroje/ui20/mockup_kovanie_v1.html (scene 3 "Katalóg + sety": grouped catalog Kategória -> Výrobca -> Rada, modals "Nová položka" / "Nový set", member editor "Ako sa určí kód? / Koľko?", live expansion preview). Summary of what it proposes:
- `hardware_sets`: new set fields `use_type` (door|drawer|lift|fall|other) · `opening_mode` (classic|tipon|other) · `drawer_construction` (metal|wood|other, only drawer) · `manufacturer` · `series` · `active` (bool, default true) -> `SET_KEYS` + `normalize_sets` + `validate_set` + `snapshot_std` content detection + global library `std` bump IN THE SAME batch (R-07: older plugin -> read-only, never truncation). Old set without classification = valid "nezaradený" (resolves exactly as today — characterization test).
- Controlled lists of manufacturers and series: new small global store `hardware_taxonomy.json` (`JsonFileStore` + `.bak`, under `Materials.with_catalog_lock`, seed Hettich/Blum/Strong + series Sensys/InnoTech Atira/Quadro/AVENTOS/TANDEM…, "+ Vytvoriť"), a series belongs to exactly one manufacturer.
- `hardware_catalog`: item + `manufacturer`/`series` (category exists), record std bump; server-side grouping Kategória -> Výrobca -> Rada (ordering and truncation always from server, "no silent caps"), search expands only matches, new item highlighted (existing `pin`).
- Modals (nx_modal.js, D-15 pattern): New/Edit item (Démos search/URL on top — existing parser fills only explicit brand + Tip-On keywords, otherwise empty; field order code -> name -> price -> unit -> category -> manufacturer -> series -> note), New/Edit set (classification 1->6 contextual, auto-suggested editable name, members). Member editor: one "+ Pridať člena" -> "Ako sa určí kód?" (fixed · by slide length = `code_by_nl` · by parameter = `param_bands`) + "Koľko?" (per unit · per owner) — today's three buttons disappear, member data shape does NOT change (XOR contract; NO `code_by_height` — R4). Live expansion preview: server read-only endpoint over a sample owner (NL/count from a sample), text composed by server (pattern `explain`). Sets view = tiles with classification chips + Active/Inactive state; inactive set not offered as new default (mapping by classification = KOV-D).
- Scope OUT: resolver and default mapping by (type × opening) (KOV-D), per-height sets and axis tables (KOV-D), D-109 ratio (R-05), "newer version" snapshot notification (KOV-D), manufacturer logos, Démos inference of series/category from breadcrumb, D-111.
- Risks named by the package: UI size (split B1 data+std / B2 catalog UI / B3 set editor), collision with R-35 (full replacement without revision — must not worsen), Démos parser regressions.

KNOWN FACTS (verified in code): `HardwareSets` global library `%APPDATA%\NOXUN\Engine\hardware_sets.json` with `STD = 1`, `STD_PARAM_FORMS = 2`, `SEED_VERSION = 2`; compatibility gate R-07 (`assess_library_doc`: whitelist `SET_KEYS`/`MEMBER_KEYS`/`PARAM_BANDS_KEYS`/`BAND_KEYS`, value-type checks `bad_type?`, `library_state` :ok/:degraded/:read_only, read-only library returns EMPTY and blocks use), R-08 inter-process lock (`Materials.with_catalog_lock`), R-11 degraded `.bak`; project snapshot of sets on the model with `snapshot_std(mapping, sets)`; `save_set!/delete_set!/set_global_mapping!` with revision (optimistic lock); member kinds: `code`, `code_by_nl`, `param_bands` (XOR), `per: unit|owner`; `explain` composes server text. `HardwareCatalog`: `STD = 'noxun-hardware-catalog'`, `SCHEMA_CURRENT = 1`, `CATEGORIES`, `normalize_item`, `create_item/patch_item/delete_item` with `row_rev`, `search`, Démos `demos_preview!/create_from_demos!`, `assess!` read-only gate. Štúdio section `hw` (ui/js/hw_sets.js, ui/js/hw_catalog.js, ui/studio_dialog.rb, ui/hardware_catalog_dialog.rb as module without window); modals nx_modal.js; Escape chain nx_esc.js (R-23.1); optimistic lock lessons (#227). KOV-A1 added front classification fields (`opening_mode`, `drawer.construction`) that KOV-D will later match against set classification. `BuildPlan::GENERIC_TYPES` = leg hinge slide handle shelf_pin connector wall_hanger (no `lift` yet — KOV-E).

CONTEXT FILES TO READ FIRST: CLAUDE.md; SYSTEM/PLAN.md (KOV-B + KOV-D + KOV-H1/H2 packages); SYSTEM/STANDARD.md §6; docs/architecture/hardware.md (hardware_sets.rb — R-07/R-08/R-11 gates, hardware_catalog.rb); docs/architecture/ui-lifecycle.md (Štúdio sekcia hw, D-15 modal, Escape chain, optimistic lock); docs/architecture/materials.md (JsonFileStore, with_catalog_lock, `.bak`, degraded); noxun_engine/core/hardware_sets.rb (SET_KEYS, assess_library_doc, snapshot_std, save_set!, normalize_sets, validate_set, validate_member, explain, expand); noxun_engine/core/hardware_catalog.rb; noxun_engine/core/json_file_store.rb; noxun_engine/ui/js/hw_sets.js; noxun_engine/ui/js/hw_catalog.js; noxun_engine/ui/studio_dialog.rb (hw section callbacks); tests/pure/test_r07_kniznica_brana.rb, tests/pure/test_hardware_sets.rb.

RULES:
- Actively try to break the design: the `std` bump + `snapshot_std` content detection (what exactly makes an older plugin refuse vs. silently accept the new set fields; project snapshots on the model written by the newer plugin and opened by the older one; templates carrying `hardware_set_defs`), classification of old sets as "nezaradený" without changing expansion results (characterization), the `use_type -> generic_type` canonical map and validation matrix (lift/fall without `GENERIC_TYPES` lift), taxonomy store consistency (series -> manufacturer integrity, rename/delete cascades, seed merge, two SketchUp windows, degraded/read-only states mirrored from the other stores), catalog `manufacturer/series` fields vs Démos import, grouping/search behaviour with 500+ items, modal validation vs server authority, optimistic-lock regressions in the set editor (pinned revision, draft survival), the live expansion preview endpoint (read-only, generation/guid guards), KOV-D readiness (does the classification data model support the later mapping key `slide|hinge|lift × opening_mode [× drawer_construction]` without another std bump?).
- Numbered findings, each labeled BLOCKER / FIX-IN-KOVB / NOTE, with file:line references and a one-line suggested resolution. Recommend concrete slicing (B1/B2/B3) with exit criteria per slice — the repo's "3 review rounds" rule punishes oversized PRs.
- Do NOT change any files. Output findings only, most severe first.
